/**
 * @param backgroundColor the backgroundColor of the top bar window
 * @param textColor the text color of the name of the window dans the control of the window (close, reduce)
 */
interface TitleBarPropsType {
  backgroundColor: string,
  textColor: string,
}

interface TitleBarStateType {}
